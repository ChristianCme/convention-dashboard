import streamlit as st

st.set_page_config(
    page_title="Votes By Resolution",
)

st.write("# Votes by Resolution")


st.markdown(
    """
    This page might be a bit redundant with votes by chapter. Thinking of more ideas still

    Ideas for charts/stats (not in this order):
    - distribution of chapters by percentage (see Henry DeGroot's tweets)
    """
)